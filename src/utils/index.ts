export interface Events {
  title: string;
  start: string;
  end: string;
  backgroundColor?: string;
  borderColor?: string;
  textColor?: string;
}

export interface DB_Events {
  id: number;
  title: string;
  start_date: string;
  end_date: string;
  backgroundColor?: string;
  borderColor?: string;
  textColor?: string;
}
